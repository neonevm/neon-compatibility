export enum Networks {
  local = 'local',
  devnet = 'devnet',
  testnet = 'testnet',
  internal_testnet = 'teststand',
  neonswap_live = 'neonswap.live',
  aws = 'aws',
  ropsten = 'ropsten',
  rinkeby = 'rinkeby'
}
